{
  "name": "video-library-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Video Library API",
  "main": "src/index.js",
  "scripts": {
    "build:lambda": "./scripts/build-lambda-package.sh",
    "validate:lambda": "./scripts/validate-lambda-package.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  }
}
